import React, { Component } from 'react';

class Testpage extends Component {
    render() {
        return (
            <div>
                This ts the Test component
            </div>
        );
    }
}

export default Testpage;